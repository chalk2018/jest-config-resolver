const jest=require("jest"),path=require("path"),configResolver=require("./jest-config-resolver"),testConfig=require(path.resolve("test.config.js")),jestConfig=configResolver(testConfig),run=()=>{jest.runCLI({config:JSON.stringify(jestConfig)},[path.resolve()]).then(e=>{}).catch(e=>{})};module.exports=run;
